import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { Camera, MapPin, Loader2, Droplets, Search } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { useAuthSession, useMizanRoles } from "@/hooks/use-auth";
import { cacheTenant, getTenantCache, queueWrite, installSync } from "@/lib/offline-sync-v2";
import { OfflineStatus } from "@/components/OfflineStatus";
import { fmtDate } from "@/lib/format";

export const Route = createFileRoute("/_authenticated/reader")({ head: () => ({ meta: [{ title: "قارئ العدادات — منصة ميزان الذكية" }] }), component: ReaderPage });
interface Subscriber { id: string; name: string; zone: string | null; meter_serial: string; tenant_id: string; }
interface ReadingRow { id: string; reading_m3: number; captured_at: string; gps_lat: number | null; gps_lng: number | null; subscriber_id: string; subscribers?: { name: string; meter_serial: string } | null; }
function ReaderPage() {
  const { user } = useAuthSession(); const { roles, profile, loading: rolesLoading } = useMizanRoles(user?.id); const canSubmit = roles.includes("meter_reader") || roles.includes("project_manager"); const tenantId = profile?.tenant_id ?? null;
  const [subs,setSubs]=useState<Subscriber[]>([]); const [rows,setRows]=useState<ReadingRow[]>([]); const [query,setQuery]=useState(""); const [subId,setSubId]=useState(""); const [reading,setReading]=useState(""); const [gps,setGps]=useState<{lat:number;lng:number}|null>(null); const [busy,setBusy]=useState(false); const [gpsBusy,setGpsBusy]=useState(false);
  const loadSubs=async()=>{if(!tenantId)return;const {data,error}=await supabase.from("subscribers").select("id,name,zone,meter_serial,tenant_id").order("meter_serial");if(!error){const value=(data??[]) as Subscriber[];setSubs(value);await cacheTenant("subscribers",tenantId,value);return;}const cached=await getTenantCache<Subscriber[]>("subscribers",tenantId);if(cached){setSubs(cached);toast.info("وضع عدم الاتصال: تم تحميل آخر نسخة محفوظة");}else toast.error("لا توجد نسخة محلية للمشتركين");};
  const loadRows=async()=>{if(!tenantId)return;const {data,error}=await supabase.from("meter_readings").select("id,reading_m3,captured_at,gps_lat,gps_lng,subscriber_id,subscribers(name,meter_serial)").order("captured_at",{ascending:false}).limit(30);if(!error){setRows((data??[]) as ReadingRow[]);await cacheTenant("readings",tenantId,data??[]);return;}const cached=await getTenantCache<ReadingRow[]>("readings",tenantId);if(cached)setRows(cached);};
  useEffect(()=>{if(!tenantId)return;void loadSubs();void loadRows();const stop=installSync(tenantId,r=>{if(r.synced){toast.success(`تمت مزامنة ${r.synced} عملية`);void loadRows();}if(r.failed)toast.error(`تعذر مزامنة ${r.failed} عملية`);});const ch=supabase.channel(`reader-live-${tenantId}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"meter_readings",filter:`tenant_id=eq.${tenantId}`},()=>void loadRows()).subscribe();return()=>{stop();void supabase.removeChannel(ch);};},[tenantId]);
  const captureGps=()=>{if(!navigator.geolocation){toast.error("GPS غير مدعوم");return;}setGpsBusy(true);navigator.geolocation.getCurrentPosition(p=>{setGps({lat:p.coords.latitude,lng:p.coords.longitude});setGpsBusy(false);toast.success("تم تسجيل الموقع")},e=>{setGpsBusy(false);toast.error(e.message||"تعذر قراءة الموقع")},{enableHighAccuracy:true,timeout:10000,maximumAge:0});};
  const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!subId||!reading){toast.error("يرجى تعبئة الحقول");return;}if(!user||!tenantId){toast.error("الحساب غير مرتبط بمشروع");return;}const subscriber=subs.find(s=>s.id===subId);if(!subscriber||subscriber.tenant_id!==tenantId){toast.error("المشترك غير تابع لمشروعك");return;}const value=Number(reading);if(!Number.isFinite(value)||value<0){toast.error("القراءة غير صالحة");return;}const payload={client_operation_id:crypto.randomUUID(),tenant_id:tenantId,subscriber_id:subId,reader_id:user.id,reading_m3:value,gps_lat:gps?.lat??null,gps_lng:gps?.lng??null,captured_at:new Date().toISOString()};setBusy(true);try{if(!navigator.onLine){await queueWrite("meter_readings",tenantId,payload);toast.success("حُفظت القراءة على الهاتف وستتزامن تلقائيًا عند الاتصال");}else{const {error}=await supabase.from("meter_readings").upsert(payload,{onConflict:"client_operation_id"});if(error)throw error;toast.success("تم حفظ القراءة والتحقق منها خادميًا");void loadRows();}setReading("");setSubId("");setGps(null);}catch(err){toast.error(err instanceof Error?err.message:"فشل حفظ القراءة")}finally{setBusy(false)}};
  const selected=useMemo(()=>subs.find(s=>s.id===subId),[subs,subId]); const visible=useMemo(()=>{const q=query.trim().toLowerCase();return q?subs.filter(s=>`${s.name} ${s.meter_serial} ${s.zone??""}`.toLowerCase().includes(q)):subs},[subs,query]);
  return <div className="space-y-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-2xl font-extrabold">قراءة العدادات</h1><p className="mt-1 text-sm text-muted-foreground">Offline-first: احفظ القراءة دون إنترنت، ثم تتم المزامنة تلقائيًا.</p></div><OfflineStatus tenantId={tenantId}/></div>{!rolesLoading&&!canSubmit&&<div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-800">صلاحيات القراءة غير مفعّلة لحسابك.</div>}<div className="grid gap-6 lg:grid-cols-5"><form onSubmit={submit} className="glass rounded-2xl p-5 lg:col-span-2 space-y-3"><div className="flex items-center gap-2 text-sm font-bold"><Droplets className="h-4 w-4"/>قراءة جديدة</div><div className="relative"><Search className="pointer-events-none absolute right-3 top-3 h-4 w-4 text-muted-foreground"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="ابحث باسم المشترك أو رقم العداد" className="w-full rounded-xl border border-border bg-background py-2.5 pr-9 pl-3 text-sm"/></div><Field label="المشترك"><select required value={subId} onChange={e=>setSubId(e.target.value)} disabled={!canSubmit||busy} className="w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"><option value="">— اختر المشترك —</option>{visible.map(s=><option key={s.id} value={s.id}>{s.meter_serial} · {s.name}{s.zone?` · ${s.zone}`:""}</option>)}</select></Field><Field label="القراءة الحالية (م³)"><input required type="number" min="0" step="0.01" value={reading} onChange={e=>setReading(e.target.value)} disabled={!canSubmit||busy} dir="ltr" className="w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm"/></Field><div className="rounded-xl bg-muted/50 p-3 text-xs"><b>القراءة السابقة والاستهلاك</b><div className="mt-1 text-muted-foreground">يحسبهما الخادم من سجل العداد.</div></div><div className="flex items-center gap-2"><button type="button" onClick={captureGps} disabled={gpsBusy||!canSubmit} className="inline-flex items-center gap-1.5 rounded-xl border border-border bg-card px-3 py-2 text-xs font-semibold">{gpsBusy?<Loader2 className="h-3.5 w-3.5 animate-spin"/>:<MapPin className="h-3.5 w-3.5"/>}التقاط GPS</button>{gps&&<span className="text-[11px] text-emerald-700 num">{gps.lat.toFixed(5)}, {gps.lng.toFixed(5)}</span>}</div>{selected&&<div className="rounded-xl bg-muted/50 p-3 text-xs"><b>{selected.name}</b><div className="text-muted-foreground">عداد: {selected.meter_serial}</div></div>}<button type="submit" disabled={!canSubmit||busy} className="flex w-full items-center justify-center gap-2 rounded-xl brand-gradient px-4 py-3 text-sm font-bold text-white disabled:opacity-60">{busy?<Loader2 className="h-4 w-4 animate-spin"/>:<Droplets className="h-4 w-4"/>}حفظ القراءة</button><p className="text-[10px] text-muted-foreground"><Camera className="inline h-3 w-3"/> يمكن ربط التصوير وOCR بنفس مسار العمليات المحلي.</p></form><div className="glass rounded-2xl p-5 lg:col-span-3"><div className="mb-3 flex items-center justify-between"><b className="text-sm">أحدث القراءات</b><span className="text-[10px] text-muted-foreground">آخر 30</span></div><div className="max-h-[500px] overflow-auto"><table className="w-full text-xs"><thead className="sticky top-0 bg-card"><tr className="text-right text-muted-foreground"><th className="py-2">العداد</th><th>المشترك</th><th>القراءة</th><th>GPS</th><th>الوقت</th></tr></thead><tbody>{rows.length===0&&<tr><td colSpan={5} className="py-6 text-center text-muted-foreground">لا توجد قراءات</td></tr>}{rows.map(r=><tr key={r.id} className="border-t border-border/60"><td className="py-2 font-semibold num">{r.subscribers?.meter_serial??"—"}</td><td>{r.subscribers?.name??"—"}</td><td className="num">{r.reading_m3}</td><td className="num text-[10px] text-muted-foreground">{r.gps_lat!=null?`${r.gps_lat.toFixed(3)},${r.gps_lng?.toFixed(3)}`:"—"}</td><td className="text-muted-foreground">{fmtDate(r.captured_at)}</td></tr>)}</tbody></table></div></div></div></div>;
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><label className="mb-1.5 block text-xs font-semibold text-muted-foreground">{label}</label>{children}</div>}
